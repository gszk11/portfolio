<div class='col-md-3'>
  <h2>ユーザー情報</h2>
  <table class='table'>
    <tr>
      <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "default-image.jpg" %>
    </tr>
    <tr>
      <th>名前</th>
      <th><%= user.name %></th>
    </tr>
    <tr>
      <th>紹介</th>
      <th><%= user.introduction %></th>
    </tr>
  </table>　 
  <div class='row'>
    <%= link_to edit_user_path(user.id), class: "btn btn-secondary btn-block" do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
    <%= link_to "レビューを投稿", new_movie_path, class: "btn btn-success btn-block" %>
  </div>
</div>